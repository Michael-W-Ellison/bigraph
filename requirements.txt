# Bigraph Cryptography System Requirements

# GUI Framework (cross-platform, Android compatible)
kivy==2.2.1
kivy-garden==0.1.5

# For fallback to simpler GUI
# tkinter is built-in to Python

# Utilities
pytest==7.4.3
pytest-cov==4.1.0
